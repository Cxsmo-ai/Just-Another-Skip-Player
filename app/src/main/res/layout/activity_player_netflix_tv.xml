<?xml version="1.0" encoding="utf-8"?>
<!-- Netflix-style player layout for TV (D-pad focused) -->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/coordinatorLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- Main Player View with TV-optimized controls -->
    <com.brouken.player.dtpv.DoubleTapPlayerView
        android:id="@+id/video_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:focusable="true"
        app:time_bar_scrubbing_enabled="true"
        app:show_subtitle_button="true"
        app:use_controller="false"
        app:dtpv_controller="@id/youtube_overlay" />

    <!-- Netflix-style Skip Intro Button (TV - larger, focusable) -->
    <Button
        android:id="@+id/btn_skip_intro_netflix"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="48dp"
        android:layout_marginBottom="120dp"
        android:background="@drawable/bg_skip_button_netflix"
        android:text="Skip Intro"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textAllCaps="false"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:visibility="gone"
        android:focusable="true"
        android:nextFocusLeft="@id/video_view"
        android:nextFocusUp="@id/video_view" />

    <!-- Loading Indicator -->
    <ProgressBar
        android:id="@+id/loading"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="center"
        android:indeterminateTint="@android:color/white"
        android:visibility="gone" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
